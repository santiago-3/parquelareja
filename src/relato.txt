1. Luego de haber hecho un código javascript que "inicializaba" las actividades haciendo que las que tenían mas de cierta altura se marque como desplegables, Me encontré con el problema de que al recargar con ctrl+r se comportaba distinto que si recargaba con ctrl+f5. Esto es porque al usar ctrl+f5 recarga las imágenes, y el script que había hecho media el alto de los elementos para calcular si necesitab marcarlos como desplegables o no, por esa razón, cuando cargaba con ctrl+r, si las imagenes ya estaban en caché media el alto correctamente, pero cuando recargaba con f5 como la función se ejecutaba antes de que las imagenes terminen de cargar entonces media mal el alto de los elementos.
Me enteré de que DOMContentLoaded se ejecuta cuando todo el dom fué parseado pero no espera a que se carguen cosas como las imágenes. Para eso tuve que usar el evento 'load' sobre Window. Pero tuve que esribir addEventListener suelto.

2. Para asegurarme de que todas las imágenes estuvieran cargadas luego de haber puesto más imagenes en el DOM vía javascript. Encontré un código en stack overflow que genera una promesa por cada imágen y cuando todas esas promesas terminaron ejecuta una función.
